server {
    listen 80;
    server_name example.com;

    root /path/to/your/website;  # Adjust this to the actual path
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
