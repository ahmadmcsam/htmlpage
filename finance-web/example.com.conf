server {
    listen 80;
    server_name 127.0.0.1;

    root /path/to/your/website;  # Adjust this to the actual path
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
